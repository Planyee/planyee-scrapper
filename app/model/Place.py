class Place:

    def __init__(self, name, address, category, img_url, desc, info):
        self.name = name
        self.address = address
        self.category = category
        self.img_url = img_url
        self.desc = desc
        self.info = info